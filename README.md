# EPLQ-LBS-Privacy-server
EPLQ is a secure and efficient solution for performing spatial range queries in Location-Based Services (LBS) without compromising user privacy. It introduces the first predicate-only encryption system for inner product range checks, enabling privacy-preserving detection of whether a user's location lies within a circular query area.
